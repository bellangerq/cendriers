<% content = data.pages.home.hero %>

<section class="index-hero">
  <div class="wrapper">
    <div class="content">
      <h1><%= content.title %></h1>
      <p><%= content.description %></p>
    </div>
    <img src="<%= content.image %>" alt="" />
  </div>
</section>
