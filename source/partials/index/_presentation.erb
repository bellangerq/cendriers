<% content = data.pages.home.presentation %>

<section class="index-presentation">
  <div class="image">
    <figure>
      <img src="<%= content.image %>" alt="<%= content.caption %>" />
      <figcaption><%= content.caption %></figcaption>
    </figure>
  </div>
  <div class="info">
    <h2><%= content.title %></h2>
    <div><%= markdown(content.description) %></div>
  </div>
</section>
